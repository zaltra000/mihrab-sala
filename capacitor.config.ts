import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.mihrab.app',
  appName: 'مِحْرَاب',
  webDir: 'dist',
  bundledWebRuntime: false
};

export default config;
